# Prefer a single install line (editable app + dev tools):
#   pip install -e ".[dev]"
# See also: pyproject.toml, Makefile, tasks.py (Invoke).
#
# Core ML / data
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0,<1.10.0

# Training dependencies are optional. Install only when retraining:
#   pip install -e ".[training]"

# NLP utilities
regex>=2024.0.0

# API + UI backend
django>=5.0
pydantic>=2.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
feedparser>=6.0.11
streamlit>=1.40.0

# Experiment tracking is optional. Install when needed:
#   pip install -e ".[tracking]"

# Dev / CI
pytest>=8.0.0
pytest-django>=4.8.0
wheel>=0.46.2
